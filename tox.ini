# tox (https://tox.readthedocs.io/) is a tool for running tests
# in multiple virtualenvs. This configuration file will run the
# test suite on all supported python versions. To use it, "pip install tox"
# and then run "tox" from this directory.

[tox]
envlist = py39,py310,pre-commit,examples,docs
isolated_build = True

[gh-actions]
python =
    3.9: py39,pre-commit,docs
    3.10: py310

[testenv:docs]
deps=
    -r{toxinidir}/doc_source/requirements.txt
changedir = {toxinidir}/doc_source
whitelist_externals = /usr/bin/make
commands =
    make html


[testenv:examples]
deps =
    -r{toxinidir}/requirements.tox.txt
    notebook
    matplotlib
    ipywidgets
    seaborn
changedir = {toxinidir}/notebooks
whitelist_externals = /usr/bin/make
commands =
    make

[testenv:pre-commit]
skip_install = true
deps = pre-commit
commands = pre-commit run --all-files --show-diff-on-failure

[testenv:coverage]
commands = pytest --cov --cov-append --cov-report=term-missing
depends =
  {py39,py310}: clean
  report: py39,py310

[testenv:report]
deps = coverage
skip_install = true
commands =
    coverage report --omit='/private/*'
    coverage html --omit='/private/*'

[testenv:clean]
deps = coverage
skip_install = true
commands = coverage erase

[testenv]
passenv =
    GRB_LICENSE_FILE
setenv =
    PIP_EXTRA_INDEX_URL = https://download.pytorch.org/whl/cpu
whitelist_externals =
    echo
deps =
    -r{toxinidir}/requirements.tox.txt
    pytest
    pytest-subtests
    pytest-cov
commands =
    pytest
